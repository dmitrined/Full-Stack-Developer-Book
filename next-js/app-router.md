# 📚 Теория маршрутизации в Next.js (App Router + TypeScript)

### 🔸 Что такое App Router?

App Router — это система маршрутизации в Next.js, основанная на **структуре папок внутри директории `/app`**, которая пришла на смену `pages`\-based маршрутизации начиная с Next.js 13+.

___

## 🔹 Основные особенности App Router:

| Особенность | Описание |
| --- | --- |
| 📁 Папка `/app` | Заменяет `/pages`. Все маршруты создаются на основе структуры этой папки |
| `page.tsx` | Отвечает за содержимое страницы (аналогично `/pages/route.tsx`) |
| `layout.tsx` | Макет для группы страниц (например, общий Header / Navbar / Footer) |
| `template.tsx` | Альтернатива `layout`, но пересоздается при каждой навигации |
| `loading.tsx` | Компонент для отображения загрузки при ожидании данных (suspense) |
| `error.tsx` | Отображается при ошибке во вложенном маршруте или загрузке |
| `not-found.tsx` | Отображается при отсутствии страницы |
| `metadata` | API для управления мета-тегами страниц (SEO, заголовки, описания) |

___

## 🔹 Как работают маршруты

-   Любая папка в `/app` становится частью URL.
-   Файл `page.tsx` внутри этой папки — это то, что отображается в браузере.

**Пример:**

```nextjs
/app
  page.tsx               → "/"
  about/
    page.tsx             → "/about"
  blog/
    page.tsx             → "/blog"
    post/
      page.tsx           → "/blog/post"
```

___

## 🔹 Layout

Файл `layout.tsx` определяет **общую структуру для маршрутов внутри папки**, где он находится.

```nextjs
// app/layout.tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html>
      <body>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
```

📌 Вложенные `layout.tsx` работают иерархически: можно задавать макеты для конкретных разделов.

___

## 🔹 Компоненты клиента и сервера

-   По умолчанию все компоненты в `/app` — **серверные**.
-   Чтобы использовать React-хуки (`useState`, `useEffect`, `Link`, и т.д.) — компонент должен начинаться с `'use client'`.

```nextjs
"use client"; // Обязательная директива
```

___

## 🔹 Навигация

Используется компонент `Link` из `next/link`, как и раньше:

```nextjs
import Link from "next/link";

<Link href="/about">О нас</Link>;
```

⚠️ Работает только в клиентских компонентах (`'use client'`).

___

## 🔹 Типизация (TypeScript)

Next.js полностью поддерживает TypeScript из коробки. Ты можешь типизировать:

-   Props: `({ children }: { children: ReactNode })`
-   Модули: `import type { Metadata } from 'next'`
-   Страницы: нет необходимости явно указывать тип `NextPage`

___

## 🔹 Особенности App Router по сравнению с Pages Router:

| Функция | Pages Router (`/pages`) | App Router (`/app`) |
| --- | --- | --- |
| Файл-маршруты | Да | Да |
| Layout на уровне разделов | Нет | Да |
| React Server Components | Нет | Да |
| Загрузка (loading UI) | Нет | Да (`loading.tsx`) |
| Ошибки (`error.tsx`) | Нет | Да |
| Динамические маршруты | Да (`[param].tsx`) | Да (`[param]/page.tsx`) |

___

## ✅ Итого:

-   `App Router` — это новая система маршрутизации, ориентированная на **папки**, **композицию**, и **серверные компоненты**.
-   Основной принцип: **структура папок = структура маршрутов**.
-   App Router делает Next.js ближе к современным архитектурам типа React Server Components + Nested Layouts.

___
