# 🧩 Что такое компоненты в App Router?

В `app/`\-директории Next.js по умолчанию **все компоненты — серверные**. Это означает, что они рендерятся на сервере, и HTML отправляется клиенту.

Ты можешь явно указать, что компонент должен быть клиентским с помощью директивы:

```tsx
'use client'
```

___

## 🖥️ Серверные компоненты (Server Components)

### 📍 Где выполняются?

На **сервере** — не попадают в браузер.

### ⚙️ Особенности:

-   Нет доступа к `useState`, `useEffect`, `useRef`, `window`, `localStorage` и т.п.
-   Можно использовать `async/await` прямо внутри компонента.
-   Отлично подходят для работы с базой данных, API, кешированием и предварительной загрузкой данных.
-   Меньше JavaScript отправляется в браузер → лучше производительность.

### ✅ Пример:

```tsx
// app/products/page.tsx
export default async function ProductsPage() {
  const res = await fetch('https://api.example.com/products');
  const products = await res.json();

  return (
    <ul>
      {products.map(p => (
        <li key={p.id}>{p.name}</li>
      ))}
    </ul>
  );
}
```

📌 Этот компонент **никогда не попадёт в JS-бандл клиента**.

___

## 🌐 Клиентские компоненты (Client Components)

### 📍 Где выполняются?

В **браузере** (как обычный React-компонент).

### ⚙️ Особенности:

-   Имеют доступ ко всем React-хукам: `useState`, `useEffect`, `useContext` и т.д.
-   Могут быть интерактивными: модалки, кнопки, формы, переключатели и т.п.
-   Обязательно помечаются директивой `'use client'` вверху файла.

### ✅ Пример:

```tsx
'use client'

import { useState } from 'react';

export default function Counter() {
  const [count, setCount] = useState(0);

  return (
    <button onClick={() => setCount(count + 1)}>
      Кликнули {count} раз
    </button>
  );
}
```

📌 Этот компонент будет загружен как часть JS-бандла и выполнен в браузере.

___

## 🤝 Как они работают вместе?

Ты можешь **вкладывать клиентские компоненты внутрь серверных**, но **не наоборот**:

```tsx
// Серверный компонент
import Counter from './Counter'; // клиентский компонент

export default function Page() {
  return (
    <div>
      <h1>Продукт</h1>
      <Counter /> {/* интерактивный компонент */}
    </div>
  );
}
```

___

## 🧠 Почему это важно?

| Серверные компоненты | Клиентские компоненты |
| --- | --- |
| Меньше JS | Интерактивность |
| Легко получать данные | Доступ к браузерным API |
| Быстрее первая загрузка | Медленнее, больше JS |
| Не имеют состояния/эффектов | Полный React-функционал |

___

## ⚠️ Частые ошибки

-   ❌ Использовать `useEffect` в серверном компоненте → будет ошибка
-   ❌ Пытаться импортировать серверный компонент внутрь клиентского
-   ❌ Забыть `'use client'`, если хочешь использовать хуки

___

## 📌 Резюме

| Характеристика | Серверный компонент | Клиентский компонент |
| --- | --- | --- |
| Где выполняется | На сервере | В браузере |
| Хуки React | ❌ Нет | ✅ Да |
| Интерактивность | ❌ Нет | ✅ Да |
| Доступ к `fetch`, DB | ✅ Да | ❌ Нет |
| Нужно ли `'use client'` | ❌ Нет | ✅ Да |

## 🚀 Поведение билда и рендеринга в Next.js (App Router)

## 🚧 Когда происходит билд?

### 🛠️ 1. Build-time

Происходит при выполнении команды:

```bash
next build
```

В этот момент Next.js:

-   Компилирует серверные и клиентские компоненты
-   Генерирует статические страницы (если используется SSG)
-   Упаковывает JS/CSS/Assets

📌 Это выполняется **один раз перед деплоем**.

___

## 🕐 2. Когда рендерятся страницы после билда?

| Стратегия | Когда происходит рендеринг |
| --- | --- |
| **SSG** | При билде (`next build`) |
| **SSR** | При каждом запросе |
| **ISR** | При первом запросе или по таймеру |
| **Client-side** | В браузере после загрузки |

___

## 🧠 Как влияет `fetch()`?

### ✅ SSG (на этапе билда)

```tsx
await fetch("https://api.example.com/data", {
  next: { revalidate: false }, // или не указывать вовсе
});
```

📌 Данные загружаются и кешируются при билде. Обновление — только через новый билд.

___

### 🔁 ISR (регенерация по таймеру)
    
```tsx
await fetch("https://api.example.com/data", {
  next: { revalidate: 60 }, // обновление каждые 60 сек
});
```

📌 Страница генерируется один раз, затем пересобирается в фоне.

___

### 🚀 SSR (на каждый запрос)

```tsx
await fetch("https://api.example.com/data", {
  cache: "no-store",
});
```

📌 Рендер каждый раз, как в классических серверах.

___

## 🧪 Нужно ли делать `next build`?

| Изменения | Нужно ли делать `next build`? |
| --- | --- |
| Изменение кода/компонентов | ✅ Да |
| Изменение статичных данных (SSG) | ✅ Да |
| Данные с `no-store` или `revalidate` | ❌ Нет |

___

## 🧪 Как проверить стратегию рендеринга?

-   В режиме разработки (`next dev`) все страницы рендерятся каждый раз.
-   Чтобы проверить настоящее поведение:

```bash
npm run build
npm start
```
___

## 📌 Итого

| Стратегия | Билд нужен? | Когда обновляются данные |
| --- | --- | --- |
| **SSG (по умолчанию)** | ✅ Да | Только при новом билде |
| **SSR (`no-store`)** | ❌ Нет | При каждом запросе |
| **ISR (`revalidate`)** | ❌ Нет | Автоматически по таймеру |
| **Client-side** | ❌ Нет | В браузере через `useEffect` |
